#!/usr/bin/env bash
INJMON_LOG_FILE=${INJMON_LOG_FILE:-/dev/stdout}
function monitor() {
    while :
    do
        inject-dll 'C:\injector\auto.dll' 'WeChat' >${INJMON_LOG_FILE} 2>&1
        sleep 10
    done
}
monitor
